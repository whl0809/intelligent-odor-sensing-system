# Outdated acquisition snapshot

This directory preserves acquisition code and hardware utilities formerly
mixed into the classification branch. It is not the source for new training
recordings and is not included in the root installation or test suite.

Use the repository's maintained acquisition branch to collect TGS + SVM41
training data. The optional root `pipeline/run_model_pipeline.py` accepts that
checkout through `--acquisition-root`.

