# Archived acquisition package

This is the former local acquisition package retained for historical
reference. It is not installed by the root project and must not be used to
collect new training data. Use the maintained acquisition branch instead.
